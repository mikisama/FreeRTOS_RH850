{
    "configurations": [
        {
            "name": "GCC",
            /* vscode is only compatible with GCC's compile_commands.json. */
            "compileCommands": "build/compile_commands.json"
        }
    ],
    "version": 4
}
